<div class= "row">
    <div class= "col my-3 align-center">
        <a href= "/post/{{id}}">
        <h2>{{title}}</h2>
        </a>
        <h3>Post created on {{format_date createdAt}} at {{format_time createdAt}} by {{user.name}}</h3>
        </div>
    </div>