<div class= "row justify-content-center">
    
        <div class="card w-75 my-2">
        <a href= "/post/{{id}}">
        <div class= "card-header">{{title}}</div>
        </a>
        <div class= "card-body">Post created on {{format_date createdAt}} at {{format_time createdAt}} by {{user.name}}</div>
        </div>
        
</div>